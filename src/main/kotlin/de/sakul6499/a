float diameter = 10F;

float lx = loc.blockX;
float ly = loc.blockY;
float lz = loc.blockZ;

double radius = diameter / 2;
double ls = Math.sqrt((lx*lx) + (ly*ly) + (lz*lz));

for(int x = lx - radius; x <= lx + rad; x++) {
    for(int y = ly - radius; y <= ly + rad; y++) {
        for(int z = lz - radius; z <= lz + rad; z++) {
            double s = Math.sqrt((x*x) + (y*y) + (z*z));

            if(s <= ls) {
                // SET BLOCK
            }
        }
    }
}